[build]
buildCommand = "cd frontend && npm ci && npm run build"

[deploy]
startCommand = "./frontend/node_modules/.bin/serve -s ./frontend/dist -l $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
